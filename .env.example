# ============================================================
# Kamakhya Prasad Computer Institute — Environment Variables
# Copy this file to .env and fill in your values.
# ============================================================

# ── Database ─────────────────────────────────────────────────
# For local development you can use a local PostgreSQL instance:
#   DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/kpci?schema=public"
#
# For Vercel / Netlify use a free cloud PostgreSQL provider:
#   • Neon     → https://neon.tech   (recommended, free tier)
#   • Supabase → https://supabase.com
#   • Railway  → https://railway.app
#
# Neon tip: use the direct (non-pooled) URL here.
# If you need connection pooling (pgbouncer), add a directUrl field to
# prisma/schema.prisma and set DIRECT_URL to the non-pooled URL.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# ── NextAuth ──────────────────────────────────────────────────
# The public URL of your deployed app (no trailing slash).
# Examples:
#   Vercel:  https://your-project.vercel.app
#   Netlify: https://your-project.netlify.app
#   Custom:  https://www.kpci.edu.in
NEXTAUTH_URL="http://localhost:3000"

# Generate a strong random secret with:  openssl rand -base64 32
NEXTAUTH_SECRET="replace-me-with-a-random-secret"

# ── Cloudinary (required for file uploads in production) ──────
# Sign up free at https://cloudinary.com
# 1. Create an unsigned upload preset (Settings → Upload → Upload presets)
# 2. Copy your Cloud Name from the Dashboard
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_UPLOAD_PRESET=""
